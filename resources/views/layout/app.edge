<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Mon blog</title>
  <link rel="stylesheet" href="{{ asset('bootstrap-css') }}">
</head>

<body>
  <nav class="navbar navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="{{ route('home') }}">AdnonisJS</a>

      @if(await auth.check())
      <div class="dropdown">
        <button class="btn btn-warning dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
          {{ auth.user.full_name }}
        </button>
        <ul class="dropdown-menu">
          <li>
            <form action="{{ route('auth.logout') }}" method="POST">
              <button class="dropdown-item">Se déconnecter</button>
            </form>
          </li>
        </ul>
      </div>

      @else
      <a href="{{ route('auth.index') }}" class="btn btn-warning">Se connecter</a>

      @endif

      <a href="{{ route('contact.index') }}" class="btn btn-light">Contactez-moi</a>
    </div>
  </nav>
  <div class="container my-2">
    @!component('components/alert')

    @!section('body')
  </div>
</body>

<script src="{{ asset('bootstrap-js') }}"></script>
</html>
